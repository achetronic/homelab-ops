rawResources:
  gateway-production:
    enabled: true
    #nameOverride: gateway-production
    apiVersion: gateway.networking.k8s.io/v1
    kind: Gateway
    spec:
      spec:
        addresses:
          - value: istio-gateway-production.istio.svc.cluster.local
            type: Hostname
        gatewayClassName: istio
        infrastructure:
          labels: {}
        listeners:
          - allowedRoutes: &allowedRoutes
              namespaces:
                from: "All"
            name: http
            port: 80
            protocol: HTTP

          - name: https-terminate
            hostname: '*.internal.place'
            protocol: HTTPS
            port: 443
            allowedRoutes: *allowedRoutes
            tls:
              mode: Terminate
              certificateRefs:
                - kind: Secret
                  group: ""
                  name: https-terminate-tls