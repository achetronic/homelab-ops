apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

replicas:
  - name: task-pv
    count: 3

commonLabels:
  app: test

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
