kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: matter-server-data
spec:
  storageClassName: standard-nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
